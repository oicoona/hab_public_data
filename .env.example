# ============================================
# HAB Public Data - Docker Compose 환경 변수
# ============================================
# 이 파일을 .env로 복사하여 사용하세요
# cp .env.example .env

# --------------------------------------------
# PostgreSQL 설정
# --------------------------------------------
POSTGRES_USER=hab_user
POSTGRES_PASSWORD=hab_password_change_me
POSTGRES_DB=hab_public_data
POSTGRES_PORT=5432

# --------------------------------------------
# Redis 설정
# --------------------------------------------
REDIS_PORT=6379
CACHE_TTL=3600

# --------------------------------------------
# FastAPI 백엔드 설정
# --------------------------------------------
BACKEND_PORT=8000
BACKEND_RELOAD=false
ALLOWED_ORIGINS=http://localhost:8501,http://streamlit:8501

# --------------------------------------------
# Streamlit 프론트엔드 설정
# --------------------------------------------
STREAMLIT_PORT=8501
BACKEND_URL=http://backend:8000

# --------------------------------------------
# Flower (Celery 모니터링) 설정
# --------------------------------------------
FLOWER_PORT=5555

# --------------------------------------------
# 보안 및 인증 (추후 추가)
# --------------------------------------------
# SECRET_KEY=your-secret-key-here
# JWT_ALGORITHM=HS256
# JWT_EXPIRATION=3600

# --------------------------------------------
# 로깅 설정
# --------------------------------------------
# LOG_LEVEL=INFO
# LOG_FORMAT=json

# --------------------------------------------
# 참고사항
# --------------------------------------------
# 1. Anthropic API 키는 여기에 저장하지 않습니다.
#    -> env/.env 파일에 claude_api_key로 설정하거나
#    -> Streamlit UI에서 직접 입력합니다.
#
# 2. 프로덕션 환경에서는 강력한 비밀번호 사용 필수!
#
# 3. ALLOWED_ORIGINS는 CORS 설정을 위해 필요합니다.
#    프론트엔드 도메인을 추가하세요.
