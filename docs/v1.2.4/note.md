# .env 에 Anthropic API Key 값이 있으면 먼저 체크해서 반영.
# 이전 시각화 지리적분포 지도표시가 사라지는 문제. 첫 데이터에 위치 경도 있는 경우 지도표시 잘 됨. 새로 데이터를 불러오면 이전 탭에 나타난 지도 시각화는 사라지고 신규 탭 지도표시만 나타나는 문제 수정.
# 데이터 질의응답 탭, 질의 창이 입력 후 아래 위치로 내려가는 문제 발생. -> 입력창은 화면 하단에 고정 위쪽에는 질문–답변이 시간순으로 누적. 새 질문을 입력하면 이전 대화는 유지, 새 답변만 아래에 추가.
# 데이터 질의응답 탭의 데이터셋 요약 행수가 3개에서 -> 10개로 변경 (수정 완료)

