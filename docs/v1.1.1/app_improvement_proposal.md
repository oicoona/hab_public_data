# 대구 공공데이터 시각화 앱 개선 제안서 (v1.1 → v1.1.1)

**문서 버전**: v1.1.1
**작성일**: 2025-12-02
**참고 문서**: `docs/v1.1.1/notes.md`

---

## 1. 개요

본 문서는 대구 공공데이터 시각화 앱 v1.1의 현재 상태(AS-IS)와 v1.1.1에서 목표하는 개선 상태(TO-BE)를 비교 분석한다.

---

## 2. 기능별 AS-IS / TO-BE 비교

### 2.1 사이드바 모델 선택

| 구분 | AS-IS (v1.1) | TO-BE (v1.1.1) |
|:-----|:-------------|:---------------|
| 모델 후보 | 구버전 모델 목록 | 최신 모델 목록으로 업데이트 |
| 선택 가능 모델 | Claude Sonnet 4, Opus 4, Haiku 3.5 | Claude Sonnet 4.5, Claude Opus 4.5, Claude Haiku 4.5 |
| 참고 문서 | - | https://platform.claude.com/docs/en/get-started#python |

---

### 2.2 챗봇 데이터 분석 방식

| 구분 | AS-IS (v1.1) | TO-BE (v1.1.1) |
|:-----|:-------------|:---------------|
| 분석 방식 | DataFrame 일부 정보 + 샘플링 데이터를 프롬프트에 포함 | Tool Calling 기법 적용 |
| 한계 | 샘플링으로 인해 복잡한 질의 응답 불가 | 15개 분석 도구로 복잡한 질문 대응 가능 |
| 데이터 접근 | 제한적 (프롬프트 크기 제약) | 도구를 통한 전체 데이터 접근 |
| 분석 정확도 | 샘플 기반 추정 | 실제 데이터 기반 정확한 분석 |

#### 2.2.1 Tool Calling 도구 목록 (신규)

`data/*.csv` 분석을 위한 15개 도구:

| 번호 | 도구명 | 설명 |
|:-----|:-------|:-----|
| 1 | `get_dataframe_info` | DataFrame 기본 정보 (행/열 수, 컬럼명, 데이터 타입) |
| 2 | `get_column_statistics` | 특정 컬럼의 통계 정보 (평균, 중앙값, 표준편차 등) |
| 3 | `get_missing_values` | 결측치 현황 분석 |
| 4 | `get_value_counts` | 범주형 컬럼의 값 분포 |
| 5 | `filter_dataframe` | 조건에 따른 데이터 필터링 |
| 6 | `sort_dataframe` | 특정 컬럼 기준 정렬 |
| 7 | `get_correlation` | 수치형 컬럼 간 상관관계 분석 |
| 8 | `group_by_aggregate` | 그룹별 집계 연산 (합계, 평균, 개수 등) |
| 9 | `get_unique_values` | 특정 컬럼의 고유값 목록 |
| 10 | `get_date_range` | 날짜 컬럼의 범위 분석 |
| 11 | `get_outliers` | 이상치 탐지 (IQR 기반) |
| 12 | `get_sample_rows` | 조건부 샘플 데이터 추출 |
| 13 | `calculate_percentile` | 백분위수 계산 |
| 14 | `get_geo_bounds` | 위경도 데이터의 지리적 범위 |
| 15 | `cross_tabulation` | 교차표 생성 |

#### 2.2.2 Tool Calling 실행 규칙

| 항목 | 규칙 |
|:-----|:-----|
| **최대 iteration** | 3회 (하나의 쿼리에서 최대 3번의 도구 호출 가능) |
| **최소 사용** | 0회 (도구 없이 답변 가능한 경우 도구 미사용 허용) |
| **도구 선택** | 질문에 가장 적합한 도구를 순차적으로 선택 |
| **결과 누적** | 각 도구 호출 결과를 누적하여 최종 답변 생성 |

```python
# Tool Calling 흐름 예시
max_iterations = 3
for i in range(max_iterations):
    tool_call = model.decide_tool(query, previous_results)
    if tool_call is None:
        break  # 더 이상 필요한 도구 없음
    result = execute_tool(tool_call)
    previous_results.append(result)

final_response = model.generate_answer(query, previous_results)
```

#### 2.2.3 에러 응답 처리

| 상황 | 응답 메시지 |
|:-----|:-----------|
| **도구 미발견** | 질문에 적합한 분석 도구를 찾지 못함 | "현재 앱이 답변할 수 없는 질문입니다." |
| **데이터 무관 질문** | 데이터 분석과 관련 없는 질문 | "데이터와 관련 없는 질문에 대해서는 대답할 수 없습니다." |
| **도구 실행 실패** | 도구 실행 중 오류 발생 | "분석 중 오류가 발생했습니다. 다시 시도해주세요." |

```python
# 에러 응답 처리 로직
def handle_tool_response(query, tool_results):
    # 데이터와 관련 없는 질문 감지
    if is_unrelated_to_data(query):
        return "데이터와 관련 없는 질문에 대해서는 대답할 수 없습니다."

    # 3번의 iteration 후에도 적합한 도구를 찾지 못한 경우
    if not tool_results or all(r is None for r in tool_results):
        return "현재 앱이 답변할 수 없는 질문입니다."

    # 정상 응답 생성
    return generate_final_response(query, tool_results)
```

---

### 2.3 챗봇 대화 컨텍스트 관리

| 구분 | AS-IS (v1.1) | TO-BE (v1.1.1) |
|:-----|:-------------|:---------------|
| 컨텍스트 범위 | 전체 대화 이력 유지 | 데이터셋별 대화 이력 분리 |
| 데이터셋 전환 | 이전 데이터셋 대화 이력 잔존 | 선택된 데이터셋의 이력만 표시 |
| 이력 삭제 | 전체 이력 삭제 | 현재 데이터셋 이력만 삭제 |
| 데이터 구조 | 단일 대화 리스트 | 데이터셋별 대화 딕셔너리 |

#### 2.3.1 컨텍스트 관리 구조 (신규)

```python
# AS-IS
st.session_state.chat_history = [
    {"role": "user", "content": "..."},
    {"role": "assistant", "content": "..."},
]

# TO-BE
st.session_state.chat_history = {
    "cctv": [
        {"role": "user", "content": "..."},
        {"role": "assistant", "content": "..."},
    ],
    "security_light": [
        {"role": "user", "content": "..."},
        {"role": "assistant", "content": "..."},
    ],
    # ... 데이터셋별 분리
}
```

---

### 2.4 챗봇 응답 스트리밍

| 구분 | AS-IS (v1.1) | TO-BE (v1.1.1) |
|:-----|:-------------|:---------------|
| 응답 출력 방식 | 전체 응답 완료 후 한 번에 출력 | 스트리밍 방식으로 실시간 출력 |
| 사용자 경험 | 긴 대기 시간 후 전체 텍스트 표시 | 응답 생성 과정을 실시간 확인 |
| 체감 응답 속도 | 느림 | 빠름 (첫 토큰 즉시 표시) |

#### 2.4.1 스트리밍 구현 방안

```python
# AS-IS
response = client.messages.create(...)
st.write(response.content)

# TO-BE
with st.chat_message("assistant"):
    with client.messages.stream(...) as stream:
        response = st.write_stream(stream.text_stream)
```

---

### 2.5 데이터 재사용 및 캐싱 최적화

| 구분 | AS-IS (v1.1) | TO-BE (v1.1.1) |
|:-----|:-------------|:---------------|
| 데이터 저장 | `session_state.datasets`에 DataFrame 저장 | 동일 (유지) |
| 지도 객체 | 탭 전환/인터랙션마다 새로 생성 | `session_state`에 지도 객체 캐싱 |
| 차트 객체 | 매번 새로 생성 | `@st.cache_data` 적용으로 캐싱 |
| 데이터셋 정보 | `get_dataset_info()` 매번 호출 | 결과를 `session_state`에 캐싱 |

#### 2.5.1 캐싱 전략

```python
# AS-IS: 매번 지도 생성
map_obj = create_folium_map(df, lat_col, lng_col, ...)
st_folium(map_obj, ...)

# TO-BE: 지도 객체 캐싱
cache_key = f"map_{dataset_name}_{len(df)}"
if cache_key not in st.session_state:
    st.session_state[cache_key] = create_folium_map(df, lat_col, lng_col, ...)
st_folium(st.session_state[cache_key], returned_objects=[])  # 인터랙션 비활성화로 리렌더링 방지
```

#### 2.5.2 `st_folium` 인터랙션 분리

| 문제 | 원인 | 해결 방안 |
|:-----|:-----|:---------|
| 줌 인/아웃 시 전체 리렌더링 | `st_folium`이 인터랙션을 Streamlit 상태 변경으로 인식 | `returned_objects=[]` 설정으로 상태 변경 방지 |
| 탭 전환 시 지도 재생성 | 캐싱 없이 매번 `create_folium_map()` 호출 | 지도 객체를 `session_state`에 저장 |

---

## 3. 코드 품질 개선 (버그/Warning 수정)

### 3.1 ZeroDivisionError 방지

| 위치 | 문제 | AS-IS (v1.1) | TO-BE (v1.1.1) | 심각도 |
|:-----|:-----|:-------------|:---------------|:-------|
| `app.py:645` | 빈 DataFrame 시 나눗셈 오류 | `df.isnull().sum().sum() / (len(df) * len(df.columns)) * 100` | 빈 DataFrame 체크 추가 | 🔴 Error |

#### 3.1.1 수정 코드

```python
# AS-IS
missing_pct = df.isnull().sum().sum() / (len(df) * len(df.columns)) * 100

# TO-BE
total_cells = len(df) * len(df.columns)
missing_pct = (df.isnull().sum().sum() / total_cells * 100) if total_cells > 0 else 0
```

---

### 3.2 빈 DataFrame 지도 렌더링 오류

| 위치 | 문제 | AS-IS (v1.1) | TO-BE (v1.1.1) | 심각도 |
|:-----|:-----|:-------------|:---------------|:-------|
| `visualizer.py:321-326` | 빈 df일 때 mean()이 NaN 반환 | `center_lat = df_clean[lat_col].mean()` | 빈 DataFrame early return | 🔴 Error |

#### 3.2.1 수정 코드

```python
# AS-IS
df_clean = df.dropna(subset=[lat_col, lng_col]).copy()
# ... (바로 mean() 호출)
center_lat = df_clean[lat_col].mean()

# TO-BE
df_clean = df.dropna(subset=[lat_col, lng_col]).copy()
if df_clean.empty:
    # 기본 대구 중심 좌표로 빈 지도 반환
    return folium.Map(location=[35.8714, 128.6014], zoom_start=12)
center_lat = df_clean[lat_col].mean()
```

---

### 3.3 NaN 값 포맷팅 오류

| 위치 | 문제 | AS-IS (v1.1) | TO-BE (v1.1.1) | 심각도 |
|:-----|:-----|:-------------|:---------------|:-------|
| `chatbot.py:51` | 모든 값이 NaN일 때 포맷 오류 | `f"min={df[col].min():.2f}, ..."` | NaN 체크 후 안전한 포맷팅 | ⚠️ Warning |

#### 3.3.1 수정 코드

```python
# AS-IS
if pd.api.types.is_numeric_dtype(df[col]):
    stats = f"min={df[col].min():.2f}, max={df[col].max():.2f}, mean={df[col].mean():.2f}"

# TO-BE
if pd.api.types.is_numeric_dtype(df[col]):
    col_min = df[col].min()
    col_max = df[col].max()
    col_mean = df[col].mean()
    if pd.isna(col_min) or pd.isna(col_max) or pd.isna(col_mean):
        stats = "모든 값이 결측치입니다"
    else:
        stats = f"min={col_min:.2f}, max={col_max:.2f}, mean={col_mean:.2f}"
```

---

### 3.4 성능 병목 개선

| 위치 | 문제 | AS-IS (v1.1) | TO-BE (v1.1.1) | 심각도 |
|:-----|:-----|:-------------|:---------------|:-------|
| `visualizer.py:357,475` | O(n) iterrows 순회 | `for idx, row in df_clean.iterrows():` | `itertuples()` 또는 벡터화 연산 | 🟡 Performance |
| `geo.py:159,166` | O(n*m) 이중 iterrows | 이중 `iterrows()` 순회 | scipy.spatial 또는 vectorized 연산 | 🟡 Performance |

#### 3.4.1 지도 마커 생성 최적화

```python
# AS-IS
for idx, row in df_clean.iterrows():
    lat = row[lat_col]
    lng = row[lng_col]
    # ...

# TO-BE
for row in df_clean.itertuples(index=False):
    lat = getattr(row, lat_col)
    lng = getattr(row, lng_col)
    # ...
```

---

## 4. 변경 요약표

| 영역 | 변경 유형 | 내용 |
|:-----|:---------|:-----|
| 모델 선택 | 🔄 변경 | Claude 4.5 시리즈로 모델 목록 최신화 |
| 챗봇 분석 | 🔧 개선 | Tool Calling 기법 도입 (15개 도구, 최대 3회 iteration) |
| 에러 응답 | ➕ 추가 | 도구 미발견/데이터 무관 질문 시 안내 메시지 |
| 대화 컨텍스트 | 🔄 변경 | 데이터셋별 대화 이력 분리 관리 |
| 응답 출력 | 🔧 개선 | 스트리밍 방식 응답 출력 |
| 데이터 캐싱 | 🔧 개선 | 지도/차트 객체 캐싱으로 병목 해소 |
| 지도 인터랙션 | 🔧 개선 | `returned_objects=[]`로 리렌더링 방지 |
| 코드 품질 | 🐛 수정 | ZeroDivisionError, NaN 처리, 빈 DataFrame 처리 |
| 성능 | 🔧 개선 | iterrows → itertuples 변환 |

---

## 5. 구현 우선순위

### 🔴 P0 - 버그/에러 수정 (즉시)

| 순위 | 항목 | 파일 | 설명 |
|:-----|:-----|:-----|:-----|
| 0-1 | ZeroDivisionError 수정 | `app.py:645` | 빈 DataFrame 체크 |
| 0-2 | 빈 지도 렌더링 오류 | `visualizer.py:321` | early return 추가 |
| 0-3 | NaN 포맷팅 오류 | `chatbot.py:51` | NaN 체크 후 포맷 |

### 🔴 P1 - 핵심 기능 (필수)

| 순위 | 항목 | 설명 |
|:-----|:-----|:-----|
| 1 | 모델 목록 최신화 | Claude 4.5 시리즈 선택 가능하도록 업데이트 |
| 2 | Tool Calling 도입 | 15개 분석 도구 구현 |
| 3 | 데이터셋별 컨텍스트 분리 | 대화 이력 독립 관리 |

### 🟡 P2 - 성능 개선

| 순위 | 항목 | 설명 |
|:-----|:-----|:-----|
| 4 | 지도 객체 캐싱 | `session_state`에 지도 저장 |
| 5 | `st_folium` 인터랙션 분리 | `returned_objects=[]` 설정 |
| 6 | 데이터셋 정보 캐싱 | `get_dataset_info()` 결과 캐싱 |
| 7 | iterrows → itertuples | 지도 마커 생성 최적화 |

### 🟢 P3 - 사용자 경험 개선

| 순위 | 항목 | 설명 |
|:-----|:-----|:-----|
| 8 | 스트리밍 응답 | 실시간 응답 출력 구현 |

---

## 6. 다음 단계

1. **P0 구현**: 버그/에러 즉시 수정 (ZeroDivisionError, NaN 처리)
2. **P1 구현**: 모델 최신화, Tool Calling 15개 도구 개발
3. **P2 구현**: 캐싱 전략 적용, 성능 최적화
4. **P3 구현**: 스트리밍 응답 구현
5. **테스트**: 전체 기능 통합 테스트
6. **문서화**: 사용자 가이드 업데이트
